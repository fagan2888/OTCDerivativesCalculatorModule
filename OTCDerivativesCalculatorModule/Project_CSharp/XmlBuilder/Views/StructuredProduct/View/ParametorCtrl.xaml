<UserControl x:Class="XmlBuilder.Views.StructuredProduct.View.ParametorCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Label Content="- Underlying" />
            <DataGrid>
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=Num_}" Header="No" />
                    <DataGridTextColumn Binding="{Binding Path=Num_}" Header="Name" />
                    <DataGridTextColumn Binding="{Binding Path=Symbol_}" Header="Drift" />
                    <DataGridTextColumn Binding="{Binding Path=Symbol_}" Header="Dividend" />
                    <DataGridTextColumn Binding="{Binding Path=Symbol_}" Header="Sigma" />
                </DataGrid.Columns>
            </DataGrid>
            <Label Content="- Underlying Correlation" />
            
            
        </StackPanel>

    </Grid>
</UserControl>
