<UserControl x:Class="RiskMonitor.AutoCallConstReturnTriggerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel  Orientation="Vertical">
            <StackPanel Name="MainPanel_" Orientation="Horizontal">
                <Label Content="Date :" />
                <TextBox Text="{Binding Path=Date_}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Trigger :" />
                <TextBox Text="{Binding Path=Trigger_}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Return :" />
                <TextBox Text="{Binding Path=Return_}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="AverageDays :" />
                <TextBox Text="{Binding Path=AverageDays_}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
